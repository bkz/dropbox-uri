# Default landing page.
DirectoryIndex index.html

# We need support for full unicode encoding.
AddDefaultCharset utf-8

# Rewrite links and pass them to the URI opener.
RewriteEngine on
RewriteRule ^([0-9a-zA-Z_\-]+)$ open.html?$1 [L]
